package com.tpw.homeshell;

//import tpw.v3.gadget.GadgetInfo;

public class PendingAddGadgetInfo extends PendingAddItemInfo {
    /*public GadgetInfo gadgetInfo;

    public PendingAddGadgetInfo(GadgetInfo info) {
        gadgetInfo = info;
        // ##otherwise Workspace.acceptDrop() will return true
        // ##if there's a 1x1 space left, a 4x1 GadgetWidget can be add to workspace
        spanX = minSpanX = info.spanX;
        spanY = minSpanY = info.spanY;
        itemType = LauncherSettings.Favorites.ITEM_TYPE_GADGET;
        title = info.label;
    }

    @Override
    public String toString() {
        return "Gadget: " + gadgetInfo.path;
    }*/
}
